const STAT_OPTIONS = Object.freeze({
    GOOD: 'good',
    NEUTRAL: 'neutral',
    BAD: 'bad'
});

export const STAT_OPTIONS_ARRAY = [
    STAT_OPTIONS.GOOD,
    STAT_OPTIONS.NEUTRAL,
    STAT_OPTIONS.BAD,
];